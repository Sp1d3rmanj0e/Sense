/// @description Insert description here
// You can write your code in this editor

draw_self();

/*
// Get cell size to local var
var cellSize = WORLD.CELL_SIZE;

// Changeable vars
var checkLength = 0.5; // how many tiles out from the door tile to check for a player
var checkWidth  = 1; // how wide you want the check to be (in tiles also)
	
// Translate to creating the rectangle
// Compenstates for the origin being in the center
checkLength = checkLength * cellSize + cellSize / 2;
checkWidth  = checkWidth  * cellSize/2 - 1;

// Create rectangles
draw_rectangle(x - checkLength, y - checkWidth, x + checkLength, y + checkWidth, true);
draw_rectangle(x - checkWidth, y - checkLength, x + checkWidth, y + checkLength, true);

// Draw origin
draw_circle(x,y,5,true);
*/