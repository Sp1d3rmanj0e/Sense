/// @description Load existing keybind setup

// Pseudocode
// Check if a custom keybind exists
	// If so, import it and override defaults
	// Otherwise, use the defaults
	
// No custom setup detected
global.kb_keyLeft = def_keyLeft;
global.kb_keyRight = def_keyRight;
global.kb_keyUp = def_keyUp;
global.kb_keyDown = def_keyDown;
global.kb_keyInteract = def_keyInteract;